class <emptyTree><<C <root>>> < (::<todo sym>)
  class <emptyTree>::<C A><<C <todo sym>>> < (::<todo sym>)
    def bar<<todo method>>(&<blk>)
      begin
        <self>.puts("before")
        x = [1, <self>.puts(), "after", ::<ErrorNode>]
      end
    end
  end
end
